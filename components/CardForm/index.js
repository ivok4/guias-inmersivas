import React, { useState } from 'react';
import axios from 'axios';
import { Container } from './styled'

const CardForm = ({ editForm = false, editFormData = {}, handleEditEnd }) => {
    const [message, setMessage] = useState('');
    return (
        <Container>
  
        </Container>
    )
}

export default CardForm;